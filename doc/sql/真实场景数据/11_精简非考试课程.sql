-- ============================================================================
-- 11_【已物化 / DEPRECATED】精简非考试课程.sql
-- ----------------------------------------------------------------------------
-- 状态: 本脚本依赖已在 v7 KISS 减肥中被删除的字段 (course_type), 不能再执行!
-- 
-- 其清理功能已上游消化:
--   1. 「集中实践/大学体育/思政实践/物理实验」 这些不可考课程在
--      工具_生成课程数据.py 里由 _is_examable() 过滤函数拦截,
--      07 号种子在生成阶段已不会 INSERT 这些课程
--   2. v6 旧 14 条 edu_subject (id 100-113) 是「计科/日语 24 级具体课程」,
--      本身也不含不可考类型
-- 
-- 下次从零重建时, 跳过该脚本即可。保留文件仅作历史记录。
-- ============================================================================

/* v7 KISS 减肥: 以下 SQL 全部禁用 (依赖已删的 course_type 列)
-- 原 11_精简非考试课程.sql
--
-- 背景:
--   本系统是「在线考试系统」(OES), 不是「教务管理系统」(AMS).
--   在线考试系统的核心定位是: 题库 + 试卷 + 考试 + 成绩, 科目表只承载
--   "可以承载题库" 的科目. 教务系统才需要录入培养方案的全部课程.
--
-- 参考:
--   - 主流 SaaS (超星学习通/智慧树/中国大学MOOC) 采用 "教师按需创建科目" 模式,
--     从无 "全校课程库" 概念
--   - 王大王《基于 Web 在线考试系统》: tb_Lesson 只含 ID/课程名/添加时间
--   - 文献笔记《基于MySQL的在线考试系统数据库设计》: 基础数据仅含 试题分类/
--     难度分类/知识点分类, 不含完整课表
--
-- 删除范围 (5 类不可考课程, 共约 336 条):
--   1. 集中实践 (80 条):    毕业设计/课程实践/综合实习 — 答辩/作品评分
--   2. 大学体育 (224 条):   (一)(二)(三)(四) — 体测+技能考核, 无笔试
--   3. 思政课综合实践 (8 条): 实践报告+论文, 非笔试
--   4. 物理实验B (24 条):    实验报告评分, 非笔试
--
-- 安全保证:
--   执行前已校验, 上述课程零题库挂载, 删除不会丢失任何业务数据.
--   未来如需重建, 由 工具_生成课程数据.py 可一键再生.
-- ============================================================================

USE online_exam_system;

START TRANSACTION;

-- 1. 删除全部 "集中实践" 类课程 (毕业设计/课程实践/专业实习等)
DELETE FROM edu_subject WHERE course_type = '集中实践';

-- 2. 删除全部 "大学体育(一)(二)(三)(四)" — 技能考核, 不上线考
DELETE FROM edu_subject WHERE subject_name LIKE '大学体育(%';

-- 3. 删除 "思想政治理论课综合实践(*)" — 实践+论文形式
DELETE FROM edu_subject WHERE subject_name LIKE '思想政治理论课综合实践%';

-- 4. 删除 "物理实验B" — 实验报告评分, 非笔试
DELETE FROM edu_subject WHERE subject_name = '物理实验B';

COMMIT;

-- ============================================================================
-- 验证
-- ============================================================================

-- 应当为 0
SELECT COUNT(*) AS '残留集中实践'   FROM edu_subject WHERE course_type='集中实践';
SELECT COUNT(*) AS '残留大学体育'   FROM edu_subject WHERE subject_name LIKE '大学体育(%';
SELECT COUNT(*) AS '残留思政实践'   FROM edu_subject WHERE subject_name LIKE '思想政治理论课综合实践%';
SELECT COUNT(*) AS '残留物理实验B' FROM edu_subject WHERE subject_name='物理实验B';

-- 精简后的全局分布
SELECT grade AS '年级', course_type AS '类型', COUNT(*) AS '数量'
  FROM edu_subject
 WHERE grade IS NOT NULL
 GROUP BY grade, course_type
 ORDER BY grade, course_type;

SELECT COUNT(*) AS '科目总数' FROM edu_subject;
*/  -- end of v7 KISS 禁用区
